package com.inventario_hardware.inventario.repositorio;

import com.inventario_hardware.inventario.model.Switch_aps;
import com.inventario_hardware.inventario.model.Telefonos;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Switch_apsRepository extends JpaRepository<Switch_aps, Long> {
}
